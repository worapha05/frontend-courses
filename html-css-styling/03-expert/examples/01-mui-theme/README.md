# MUI Theme Demo

```bash
cd 03-expert/examples/01-mui-theme
npm install
npm run dev
```

จุดโฟกัส:

- `src/theme/harborTheme.ts` — `createTheme` + component overrides
- `src/components/Panel.tsx` — `styled`
- `src/App.tsx` — ผสม `sx` สำหรับ one-off layout
