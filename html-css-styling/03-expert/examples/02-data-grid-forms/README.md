# Data Grid & Forms Demo

```bash
cd 03-expert/examples/02-data-grid-forms
npm install
npm run dev
```

เทคนิคที่สาธิต:

- `OrdersGrid` — sort/filter + skeleton ความสูงคงที่ (`minHeight`)
- `InviteForm` — `react-hook-form` + `helperText` จองที่ลด layout shift
