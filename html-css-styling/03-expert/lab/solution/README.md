# เฉลย Lab Expert — Harbor Console

```bash
npm install
npm run dev
```

โครงสร้าง:

- `src/theme/harborTheme.ts` — createTheme + overrides
- `src/components/PageHeader.tsx` — styled header
- `src/components/OrdersTable.tsx` — skeleton + minHeight
- `src/components/InviteForm.tsx` — validation styling ลด CLS
- `index.html` — anti-flash theme script
- `NOTES.md` — SSR / คำถามคิด
