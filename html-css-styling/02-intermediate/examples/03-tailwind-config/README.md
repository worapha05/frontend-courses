# Tailwind Config Demo

```bash
cd 02-intermediate/examples/03-tailwind-config
npm install
npm run dev
```

จุดที่ควรดู:

- `tailwind.config.js` — `theme.extend`, `darkMode: "class"`, `screens.xs`
- `src/main.js` — toggle class `dark` บน `<html>`
- arbitrary value: `bg-[conic-gradient(...)]`
