# เฉลย Lab Beginner — Nori Press

เปิด `index.html` ใน browser หรือรัน:

```bash
npx --yes serve .
```

ไฟล์หลัก:

- `index.html` — semantic landmarks + skip link
- `styles.css` — tokens, flex navbar, grid shell, auto-fit cards
- `NOTES.md` — คำตอบคำถามคิด
