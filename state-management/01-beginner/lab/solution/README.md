# ShopDesk Lab — Solution

โครงสร้างเฉลย:

| ไฟล์           | หน้าที่                   |
| -------------- | ------------------------- |
| `NOTES.md`     | Ownership + คำตอบคำถามคิด |
| `store/*`      | RTK UI store              |
| `orders/*`     | TanStack Query            |
| `catalog/*`    | Apollo Client             |
| `components/*` | UI ที่ผูก data flow       |

อ่านลำดับ: `NOTES.md` → store → orders → catalog → components
